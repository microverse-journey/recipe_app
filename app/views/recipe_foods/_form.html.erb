<%= form_with model:[@recipe, @recipe_food], url:recipe_recipe_foods_path(@recipe.id) do |form| %>
  <div>
    <%= form.label :food_id %>
    <%= form.collection_select :food_id, Food.all, :id, :name, required:true %>
  </div>
  <div>
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>
  <div>
    <%= form.submit 'Add ingredient' %>
  </div>
<%end%>
